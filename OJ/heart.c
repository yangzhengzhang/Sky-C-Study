#include <stdio.h>

int main(void)
{
    int t, l[50][1000], i, j, max[50];

    scanf("%d", &t);

    for (i = 0; i < t; i++)
    {
        for (j = 0; j < 1000; j++)
        {
            scanf("%d", &j);

            if (getchar == '\n')
                break;
        }
    }
}
