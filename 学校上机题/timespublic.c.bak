#include <stdio.h>

int main(void)
{
    int i, m, n;

    scanf("%d %d", &m, &n);

    for (i = 1; m * i % n != 0; i++)
        printf("%d ",i);
    printf("%d", m * i);

    return 0;
}
