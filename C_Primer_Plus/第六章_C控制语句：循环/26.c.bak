#include <stdio.h>

#define SIZE 26

int main(void)
{
    char element[SIZE];
    int i;
    for (i = 0; i < 26; i++)
        element[i] = 'a' + i;
    for (i = 0; i < 26; i++)
        printf("%c", element[i]);
    return 0;
}
