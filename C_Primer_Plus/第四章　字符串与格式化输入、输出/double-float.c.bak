#include <stdio.h>
#include <float.h>

int main(void)
{
    double i = 1.0 / 3.0;
    float j = 1.0 / 3.0;

    printf("%.4f %.4f\n", i, j);
    printf("%.12f %.12f\n", i, j);
    printf("%.16f %.16f\n", i, j);
    printf("%f %f", FLT_DIG, DBL_DIG);
    return 0;
}
